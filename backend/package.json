{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "db:migrate": "drizzle-kit migrate",
    "seed:clear:webhooks": "bun run src/db/seeds/clear-gmail-webhook-events.seed.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "axios": "^1.13.6",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.4",
    "jose": "^6.2.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv": "^17.3.1",
    "drizzle-kit": "^0.31.9",
    "tsx": "^4.21.0"
  }
}