{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "hono": "^4.12.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}